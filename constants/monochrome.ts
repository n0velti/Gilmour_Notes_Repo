/** Minimal black & white palette (no accent colors). */
export const BW = {
  bg: '#ffffff',
  fg: '#000000',
  muted: '#666666',
  border: '#000000',
  /** Light UI borders (buttons, chrome) */
  borderSoft: 'rgba(0,0,0,0.12)',
  /** Faint dividers between note blocks */
  hairline: 'rgba(0,0,0,0.08)',
} as const;
